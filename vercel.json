{
  "buildCommand": "tar -xzf toorock-source.tar.gz --strip-components=1 && npm install --legacy-peer-deps --no-audit --no-fund && npm run build",
  "outputDirectory": "dist/public",
  "framework": null,
  "cleanUrls": true,
  "headers": [
    {
      "source": "/(.*)",
      "headers": [
        { "key": "X-Content-Type-Options", "value": "nosniff" },
        { "key": "Referrer-Policy", "value": "strict-origin-when-cross-origin" }
      ]
    }
  ]
}
